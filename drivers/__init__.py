"""In-repo AgentDriver implementations (see DriverInterface.md).

Each benchmarked agent gets one driver module here. The agent code itself
stays untouched wherever it lives; the driver only adapts construction.
"""
